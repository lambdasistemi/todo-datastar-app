name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  build:
    runs-on: [self-hosted, nixos]
    steps:
      - uses: actions/checkout@v4
      - name: Build
        run: nix build --quiet

  formatting:
    runs-on: [self-hosted, nixos]
    steps:
      - uses: actions/checkout@v4
      - name: Check formatting
        run: nix develop --quiet -c fourmolu -m check src lib datastar-lucid

  hlint:
    runs-on: [self-hosted, nixos]
    steps:
      - uses: actions/checkout@v4
      - name: Run HLint
        run: nix develop --quiet -c hlint src lib datastar-lucid

  deploy:
    if: >-
      github.event_name == 'push' ||
      github.event.pull_request.head.repo.full_name == github.repository
    needs: [build, formatting, hlint]
    runs-on: [self-hosted, nixos]
    env:
      DOCKER: /run/current-system/sw/bin/docker
    steps:
      - uses: actions/checkout@v4
      - name: Build and load docker image
        run: |
          nix build .#docker-image --quiet
          $DOCKER load < result
      - name: Deploy
        env:
          IMAGE: ghcr.io/lambdasistemi/datastar-examples
          SERVICE_DIR: /opt/services/todo
        run: |
          TAG=$(nix eval .#imageTag.x86_64-linux --raw)
          $DOCKER compose -f $SERVICE_DIR/docker-compose.yaml down || true
          echo "VERSION=$TAG" > $SERVICE_DIR/.env
          $DOCKER compose -f $SERVICE_DIR/docker-compose.yaml up -d
          echo "Deployed $TAG"
